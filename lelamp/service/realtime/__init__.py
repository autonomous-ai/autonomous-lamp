"""Realtime voice agent service — audio-native brain for live conversation."""

import logging

logging.getLogger("lelamp.service.realtime").setLevel(logging.DEBUG)
logging.getLogger("lelamp.voice").setLevel(logging.DEBUG)
